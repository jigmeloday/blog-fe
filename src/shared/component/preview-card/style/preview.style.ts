import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const ProfileImage = styled(Box)(
 ({}) => `
    height: 42px;
    width: 42px;
    position: relative;
    cursor: pointer;
    overflow: hidden;
    border-radius: 46px;
` );
